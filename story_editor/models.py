"""Database models for the story editor application."""
from __future__ import annotations

# The project currently uses an in-memory representation of stories in ``views.py``.
# The module is kept to ease the transition to a persistent database model.
